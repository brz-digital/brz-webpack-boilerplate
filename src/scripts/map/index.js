class Map {
  constructor() {
    this.settings = {
      callbackName   = '__googleMapsApiOnLoad',
      options        = ['channel', 'client', 'key', 'language', 'region', 'v'],
    }
  }
}

export default Map;
